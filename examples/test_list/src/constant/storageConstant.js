/**
 * 缓存常量，列表数据避免多次请求
 */

const USER_INFO='userInfo'// 用户信息
const SESSION_ID='sessionId'//header中携带
const MODULE_ITEM='item'//组件的详情
const OPTIONS='options'//组件判断数据
const SEEDINFO='seedInfo'
const CONTEXTID='contextId'
const TEACHING_SITE = "硚口教学点"
module.exports={
  USER_INFO,
  SESSION_ID,
  OPTIONS,
  MODULE_ITEM,
  SEEDINFO,
  CONTEXTID,
  TEACHING_SITE
}
